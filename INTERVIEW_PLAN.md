# 모의 면접 (비주얼 노벨) — 계획 · 진행 체크포인트

이 파일은 작업을 **중간에 끊겼다가 이어서 하기 위한** 기록이다. 단계가 끝날 때마다 아래 체크리스트의 상태를 갱신하고 커밋한다.
새 세션에서 이어서 할 때는 이 파일 → `HISTORY.md` → `CLAUDE.md` 순으로 읽는다.

## 요구사항 (사용자 확정, 2026-09-06)
- 메인 화면(About Me 히어로)의 「메일 보내기」 버튼을 없애고 **「🎙 모의면접」** 버튼을 넣는다. (연락처는 About Me 본문에 그대로 있음)
- 버튼을 누르면 **포트폴리오 위에 오버레이**되는 비주얼 노벨이 뜬다. 보는 사람(면접관 역)이 **선택지**로 질문을 고르면 내가 답한다.
- 화자(나)는 **배너의 광부 캐릭터**(`banner.png`)로 표현.
- 흐름: **주제 → 질문 → 꼬리질문** 트리와 **주제 → 질문** 평면 구조가 섞인 형태.
- 게임 요소: **관심도(호감도) 게이지** + 물어본 질문 체크 + 답변 끝에 「관련 문서 보기」 + 게이지에 따라 갈리는 엔딩.
- 내용: 사용자가 준 `면예질.pdf`(넥슨 R&D실 대비 예상 질문·답변)를 참고해 **초안**을 쓴다. 단, **지원동기·사옥·특정 회사 부서 등 회사에 따라 달라지는 질문은 제외**하고 공통 질문 위주로. 메이플스토리 관련은 "포트폴리오 문서(유니온·길드·숨바꼭질)" 설명 질문만 남긴다.
- 사용자가 **편집기에서 시트(표) 형태로** 질문·답변을 고칠 수 있어야 한다.
- 토큰 사용량이 한계에 가까워지면 진행 상황을 이 파일에 저장하고 커밋해, 다음에 바로 이어서 할 수 있게 한다.

## 설계 (확정)
### 데이터 — `data.js` 안의 `window.PORTFOLIO_INTERVIEW`
`data.js` 에 함께 두면 편집기의 저장 경로(작업본 localStorage · 폴더 저장 · GitHub 배포 · zip) 를 전부 그대로 쓸 수 있다.
```js
window.PORTFOLIO_INTERVIEW = {
  me: { name:"이정훈", art:"banner.png" },
  interviewer: "면접관",
  intro: ["…"],                       // 시작 시 내가 하는 인사 (문단 배열)
  endings: [                          // 관심도 구간별 마무리 (높은 min 부터 검사)
    { min:70, grade:"S", title:"…", lines:["…"] }, { min:40, grade:"A", … }, { min:0, grade:"B", … } ],
  topics: [ { id:"intro", name:"자기소개", icon:"🙋", hint:"한 줄 설명" }, … ],
  questions: [
    { id:"q01", topic:"intro", q:"자기소개를 해주세요", a:["문단1","문단2"],
      follow:["q02","q03"],           // 답변 뒤에 제안할 꼬리질문 id
      sub:false,                      // true 면 주제 목록에 안 보이고 꼬리질문으로만 나옴
      link:"1-2-0",                   // 관련 문서 (섹션-그룹-문서 번호, index.html 해시)
      pts:3 }                         // 관심도 점수
  ]
};
```
### 사이트 — `index.html`
- 히어로: `heroHTML()` 의 `mailBtn` → `<button class="btn key" id="vnOpen">🎙 모의면접</button>`.
- 해시를 `u-m-d` 까지 받아 문서로 스크롤 (`route()`).
- `#vn` 오버레이(fixed, z 100): 상단바(제목 · 관심도 게이지 · 진행 n/N · ✕) / 왼쪽 캐릭터(`banner.png`, 말할 때 밝아짐) / 오른쪽 대화상자(이름표 + 타자 효과 본문) / 선택지 목록.
- 상태: 타이틀 → 주제 선택 → 질문 선택 → 면접관 질문 말풍선 → 내 답변(문단마다 클릭) → 꼬리질문·관련 문서·다른 질문 선택지 → … → 「면접 마무리」 → 엔딩(등급·요약·포트폴리오 보기·다시).
- 조작: 클릭/Space/Enter 진행, 숫자키 선택, Esc 닫기. 진행 상황은 localStorage(`pf-vn`)에 저장해 「이어하기」.
- 관련 문서 보기 → 오버레이 닫고 해당 문서로 이동.

### 편집기 — `admin.html`
- 상단 「🎙 모의면접」 버튼 → 전체 화면 시트 편집기(질문 표 / 주제 / 도입·마무리 문구 탭).
- 질문 표: 번호 · 주제 · 질문 · 답변(빈 줄로 문단 구분) · 꼬리질문(번호) · 관련 문서(선택) · 점수 · 꼬리전용 · 이동/삭제. 「+ 질문」, 검색, **TSV 복사/붙여넣기(엑셀 왕복)**.
- `data.interview` 에 저장 → 기존 `save()` 와 `buildDataJs()` 로 같이 내보내진다. 불러오기(`dataPicker`)도 `PORTFOLIO_INTERVIEW` 를 읽는다.

### 내용 초안 (주제)
1. 자기소개 · 인적성 — 자기소개, 기획자가 된 계기, 노력, 장점, 단점, 갈등, 실패, 업무 전환 적응
2. 프로젝트 깊이 파기 — 외노자(맨홀·재화 소모처·인트로 갈등·테이블 설계), 극락가신당(팀장·정보 선별 전달), 포수 시뮬레이터, 오목, The Glitch, SOS
3. 기획 철학 — 게임이란, 재밌는 게임, 좋은 기획자, 시스템 vs 콘텐츠, 타 직군 전달, UX vs 비용
4. 기술 · 툴 — Excel 테이블, Python, C# 몬스터 AI(+어려웠던 점), Figma, 도구 적응
5. 게임 취향 — 인생 게임(+적용), 좋아하는 장르(+적용), 웹툰(+적용)
6. 포트폴리오 문서 — 유니온 분석, 길드 개편안, 숨바꼭질 제안서 각각의 의도 질문들
7. 가벼운 질문 — MBTI, 여행, 커뮤니티, 마지막 한마디
제외: 지원동기, 회사/부서 관련, 사옥, 회식·야근, 결정석 등 시사 이슈, "우리 게임에" 류는 "실제 게임에 적용한다면"으로 일반화.

## 진행 체크리스트
| # | 단계 | 상태 | 비고 |
|---|---|---|---|
| 0 | 계획 문서(이 파일) · 메모리 | ✅ 완료 | |
| 1 | 내용 초안 — `PORTFOLIO_INTERVIEW` 를 `data.js` 에 추가 (스크래치 `build_interview.py`) | ✅ 완료 | 7주제 · 60문항(주제 목록 32 + 꼬리 28), 점수 합 345 |
| 2 | `index.html` — 히어로 버튼 교체 + 오버레이 VN 엔진 + 문서 해시(u-m-d) | ⬜ | |
| 3 | `admin.html` — 시트 편집기 + 저장/불러오기 통합 + 도움말 | ⬜ | |
| 4 | 문서(CLAUDE.md · HISTORY.md) · 헤드리스 캡처 검증 · 커밋 · 푸시 | ⬜ | |
| 5 | (후속) 사용자 검토 후 문구 손질, 로그 창·자동 진행 같은 편의 기능 | ⬜ | 사용자와 논의 |

## 이어서 할 때
- 단계 상태가 ⬜ 인 첫 항목부터. 스크래치 스크립트가 없으면 `data.js` 의 `PORTFOLIO_INTERVIEW` 를 직접 고친다.
- 검증: `node -e` 로 `new Function` 파싱, Chrome 헤드리스 캡처(`chrome.exe --headless=new --screenshot`, `python -m http.server 8765`).
