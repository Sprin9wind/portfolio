# 작업 이력

이 폴더에서 무엇을 언제 했는지 적어 둔다. 새 작업을 마치면 맨 아래에 한 줄 보탠다.
세부 커밋은 `git log --oneline` 으로, 작업 규칙은 [CLAUDE.md](CLAUDE.md) 로.

## 한눈에

| 날짜 | 한 일 |
|---|---|
| 2026-08-25 | 포트폴리오 사이트 첫 업로드. `index.html` + `admin.html` + `data.js` 구조 |
| 2026-08-31 | 편집기로 내용 여러 차례 수정·내보내기 (커밋 12회) |
| 2026-09-04 | 파일 재업로드 |
| 2026-09-06 | 디자인 개편 → 편집기 대폭 개선 → 노션 원문으로 내용 전면 교체 (아래 상세) |

## 2026-09-06 상세

### 오전 — 디자인 개편
- `index.html` 을 크림 지면 + 레드 강조의 에디토리얼 레이아웃으로 바꿈. 다크 테마 유지.
- 프로젝트 수가 0으로 나오던 문제 수정 (`layout:"gallery"` 로 프로젝트 섹션을 찾도록).

### 편집기(admin.html) 개선 — 커밋 `0d377c5` `531bdb8` `9e40b6e` 와 이번 커밋
- 편집 칸을 실제 사이트와 같은 모양으로. 노션식 입력(`/` 메뉴, `# ` `- ` `> ` 단축, 서식 툴바).
- 이미지 병렬 배치(한 줄 최대 4장), 여러 블록 동시 선택, 왼쪽 트리 재작업(검색·접기·드래그 정렬), 표 편집 도구.
- 이미지 도구막대 위치 수정 — `#imgTools{position:fixed}` 에 `inset:0` 이 없어 문서 맨 아래 기준으로 놓이던 문제.
  지금은 이미지 **가운데 위**에 얹힌다.
- 손잡이(⠿) 드래그로 이미지 줄 합치기 — 대상 이미지의 좌·우 바깥 1/3 에 놓으면 나란히.
- 글자 크기 조절 — `A− / A+`, px 입력칸(목록 포함), `기본`, Ctrl+Shift+`.` `,`.
- **되돌리기 / 다시 실행** — 블록 삭제·이동·표 편집·이미지 배치까지 Ctrl+Z / Ctrl+Y (Ctrl+Shift+Z) 로 되돌아감.
  도구막대 맨 앞에 `↶ ↷` 버튼.
- 범위 선택 시작 지점 확장 — 편집 칸 바깥의 어두운 여백 어디서든 끌어서 블록을 고를 수 있음.
- `? 도움말`(F1) 모달 — 처음 쓰는 사람이 여기만 보고도 편집할 수 있게 전체 사용법 정리.
- 서식 툴바 버튼이 두 번 실행되던 버그(`[data-cmd]` 바인딩 범위) 수정.

### 내용 전면 교체 — 노션 원문 반영
- 원본: 노션 「2026 메커톤 Portfolio」
  https://app.notion.com/p/2026-Portfolio-0009d4ab6bba82c5b160816e3f7c494a
- 본문 문장은 **원문 그대로**, 구조만 사이트 블록으로 옮김. 4 섹션 · 8 그룹 · 28 문서.
  - About Me: 소개 · 연락처 · 교육/자격증 · Tools(6종, 숙련도)
  - 개발 프로젝트: 외(계인)노(동)자 8문서 · 극락가신당 4문서 · 모션인식 포수 시뮬레이터 3문서
  - 메이플스토리 기획 문서: 메이플 프론티어 제안서 4문서 · 유니온 시스템 분석 · 길드 시스템 개선
  - 대외 활동: 4건
- 이미지 52장 전부 내려받아 `images/` 에 넣음 (`alien-*`, `shrine-*`, `catcher-*`, `frontier-*`, `union-*`, `guild-*`).
  폭 2000px 초과분은 2000px 으로 축소. 프로필 사진은 노션 원본(577×741)으로 교체.
- 노션에서 API 로 받을 수 없는 것: 포수 시뮬레이터의 mp4 2개, 유니온·길드 문서의 PDF·xlsx 첨부.
  본문에 노션 원문 링크로 안내해 두었음. 저장소에 직접 넣으려면 노션에서 수동으로 내려받아 `files/` 같은 폴더에 두고 링크를 바꾸면 된다.
- 유튜브 영상 2개(외노자 플레이 영상, 포수 시뮬레이터 플레이 영상)는 본문에 바로 재생되도록 임베드.
- 예전 `images/01-image.png`(외노자 대표 이미지와 같은 그림), `01-______________.jpg`(프로필 중복)는 정리.

### 내용 다듬기 + 화면 정리 (같은 날, 이어서)
- 오탈자 수정(탐헙→탐험, BRIGE→Bridge, 어드벤쳐 포멧→어드벤처 포맷, MOS 표기, 번호 중복 5→6·7 등)과 용어 통일(컨텐츠→콘텐츠).
- 각 프로젝트를 기획서 구조로 재정리 — 개요 표(장르·기간·역할·담당) → 담당 업무(항목별 강조) → 성과(강조 박스) → 회고(상황·문제·개선·배운 점).
  About Me 는 줄글 대신 "이렇게 일합니다" · "핵심 역량(근거 프로젝트)" 표 · 연락처로 구조화.
- index.html: 히어로 아래 지표 밴드(숫자 3개 + 칩) 제거. 하위 탭·버튼·태그를 알약 모양에서 8px 사각형으로. 기울어진 배너 액자를 바로 세움.
- 본문 폭을 틀 전체(1080px)로 — `.doc{max-width:70ch}` 제한 해제. 편집기 편집 칸(`--doc-w`)도 같은 폭으로.

## 2026-09-06 — 「기획 프로젝트」 섹션 추가 (DEVELROCKET 부트캠프)

### 계획 (실행 전 확정)
- **자료 수집**: Google Drive 공개 폴더 2개를 재귀로 내려받아 텍스트로 변환(docx·xlsx·pptx·pdf → txt). Figma 「The Glitch」 파일은 MCP 로 구조(2,407개 1920×1080 씬 프레임)를 읽고 대표 화면 13장을 캡처(무료 플랜 호출 한도로 이 이상은 불가).
- **섹션**: `기획 프로젝트` (layout gallery) 를 개발 프로젝트 바로 다음(탭 3번째)에 추가. 그룹 2개:
  1. **The Glitch — 방탈출 추리 비주얼 노벨** (5일 · 5인 · Figma). 문서: 소개 / 기획 과정(장르·툴 선정) / 담당 ① 개발·빌드 / 담당 ② 2번째 방 「책임의 방」 / 담당 ③ 프롤로그·에필로그 / 팀 프로세스와 일정 / 회고 / ✍️ 추천 추가 사항.
     웹 빌드(Figma 프로토타입) 링크 버튼 + 본문에 안내. 스크린샷 13장(`images/glitch-*.jpg`).
  2. **SOS — Sands of Sin (로그라이트 RPG 기획서)** (2주 · 6인). 문서: 소개 / 출발점(「수명」 단일 자원 아이디어) / 전투 시스템 기획 / 몬스터 시스템 기획 / 전투 밸런스 시뮬레이터 / 회고 [초안] / ✍️ 추천 추가 사항.
     시뮬레이터는 `sim/` 폴더에 HTML + MonsterTB_1.2.xlsx 를 넣고 **자동 로드**되게 손봐 본문에 iframe 으로 임베드 + 새 창 링크.
     팀원 이름은 R&R 표에 그대로, 팀원이 만든 UI·몬스터 아트는 **출처 표기**해 사용.
- **작성 원칙**: 내 문서(제안서·회고록·기획서·일일보고서·회의록)의 사실만 쓰고, 기획 의도와 결정 근거를 앞세워 기획서 톤으로 정리. 개인 회고가 없는 SOS 는 문서 근거로 [초안] 표시. 각 그룹 맨 끝에 사용자가 직접 채울 「추천 추가 사항」 문서를 둔다.
- **검증**: index/admin/data 파싱 검사 + 이미지 참조 검사 → 커밋 → 푸시.

### 결과
- `data.js` 에 「기획 프로젝트」 섹션 추가 — The Glitch 8문서, SOS 7문서 (총 5 섹션 · 10 그룹 · 43 문서).
- `images/glitch-01~13.jpg` (Figma 캡처, 1600px JPEG), `images/sos-ui-*.jpg` 6장, `images/sos-mon-*.jpg` 9장, `images/sos-worldmap.jpg` — 약 5MB.
- `sim/atb-battle-simulator.html` — 원본 시뮬레이터에 `loadWorkbook()` 을 분리하고 같은 폴더의 `MonsterTB_1.2.xlsx` 를 자동 로드하도록 손봄. 전투 밸런스 시뮬레이터 문서에 iframe 임베드.
- 두 그룹 맨 끝에 「✍️ 추천 추가 사항 (직접 편집)」 문서. SOS 회고는 [초안] 표시하고 '느낀 점'은 [직접 작성] 자리로 남김.
- 확인 필요: 김진용 회고록에는 레벨 검토 당시 '2번째 방'을 박원균이 기획했다는 언급이 있음(배치 순서가 바뀌었을 가능성). 본문은 이정훈 회고록(프롤로그·에필로그·2번째 방 담당)과 최종 빌드의 R2 프레임 기준으로 썼으니, 실제 담당 방이 다르면 문서 제목과 본문을 고칠 것.
- 자료 원본은 Drive 링크로만 연결(저장소에는 넣지 않음). 세션 스크래치 `scratchpad/drive/` 에 내려받은 원본과 txt 변환본이 있음.

## 2026-09-06 — 영상 링크를 썸네일 카드로
- 레퍼런스 사이트 방식으로: 「▶ 플레이 영상」 빨간 버튼과 본문 유튜브 iframe 을 **썸네일 + 재생 아이콘 카드**(`.vid`)로 바꿈. 누르면 새 창에서 영상.
  그룹 머리말·문서 상단의 `links` 중 유튜브 주소는 자동으로 카드가 되고, PDF 등 다른 링크는 그대로 버튼.
- 편집기에 `▶ 영상` 버튼(`/영상`) 추가 — 주소·이름만 넣으면 카드가 들어간다. 클릭 = 선택, 더블클릭 = 수정. 도움말에도 추가.
- 확인: Chrome 헤드리스 캡처로 사이트(그룹 머리말·본문)와 편집기 화면 모두 확인.

## 2026-09-06 — 모의면접 (비주얼 노벨) 추가
- 히어로의 「메일 보내기」를 「🎙 모의면접」으로. 누르면 포트폴리오 위에 비주얼 노벨이 오버레이된다(`vn.js`).
  보는 사람이 면접관이 되어 주제 → 질문 → 꼬리질문을 선택지로 고르고, 배너의 광부 캐릭터(나)가 타자 효과로 답한다.
  관심도 게이지 · 물어본 질문 체크 · 「관련 문서 보기」(해시 `섹션-그룹-문서`) · 관심도 구간별 엔딩(S/A/B) · 이어하기.
- 내용은 사용자의 `면예질.pdf`(면접 예상 질문·답변)를 회사 특정 질문 빼고 일반화해 7주제 60문항 초안으로 넣음(`data.js` 의 `PORTFOLIO_INTERVIEW`).
  숨바꼭질 제안서 문답은 포트폴리오에 해당 문서가 없어 제외.
- 편집기에 「🎙 모의면접」 시트(`admin-vn.js`): 질문 표 · 주제 · 인사/마무리 탭, TSV 복사/붙여넣기(엑셀 왕복), 그 자리에서 미리보기. 도움말에 사용법 추가.
- 계획·체크리스트는 `INTERVIEW_PLAN.md`. 확인은 Chrome 헤드리스 캡처(사이트 각 단계, 모바일, 편집기 시트·미리보기).

## 다음에 이어서 할 만한 것
- 모의면접 문구 검토(사용자), 면접관 리액션 문구·대사 로그 창·자동 진행 같은 편의 기능(원하면).
- 브라우저에서 편집기 클릭 테스트 (자동화 환경이 막혀 코드 검토만 했음).
- 노션 첨부 파일을 저장소에 직접 넣기.
- 업로드된 PDF 포트폴리오와 내용 대조.
