# CLAUDE.md

이 파일은 Claude Code가 이 저장소에서 작업할 때 참고하는 규칙과 맥락입니다.

## 프로젝트 개요

게임 기획자 **이정훈**의 개인 포트폴리오 웹사이트. 빌드 도구·프레임워크·패키지 매니저를
쓰지 않는 **순수 정적 사이트**이며, GitHub Pages로 배포된다.

- 배포 저장소: `Sprin9wind/portfolio` (branch `main`, 폴더 `/` root)
- 공개 주소: `https://sprin9wind.github.io/portfolio/`
- 편집기 주소: `https://sprin9wind.github.io/portfolio/admin.html`
- 사용자는 개발자가 아니다. 터미널·빌드·의존성 설치를 요구하는 해법은 채택하지 말 것.

## 파일 구조

```
portfolio/
├── index.html    보기 전용 사이트. HTML+CSS+JS 전부 인라인. 외부 의존성 0
├── admin.html    편집기. 마찬가지로 단일 파일. 내용 편집 → GitHub 커밋까지 담당
├── data.js       내용 데이터. 노션 원문을 옮긴 것. admin.html이 다시 내보내기도 한다
├── banner.png    상단 배너 일러스트 (노션 페이지 커버와 같은 그림)
├── profile.jpg   프로필 사진 (노션 원본)
├── images/       본문 이미지. 프로젝트별 접두어 — alien-* 외노자 · shrine-* 극락가신당 ·
│                 catcher-* 포수 시뮬레이터 · frontier-* 메이플 프론티어 · union-* · guild-* · 02~07 툴 로고
└── HISTORY.md    작업 이력. 새 작업을 마치면 여기에 한 줄 보탠다
```

`index.html`과 `admin.html`은 `data.js`를 `<script src="data.js">`로 읽는다.

### 내용의 원본은 노션이다

`data.js`의 문장은 노션 「2026 메커톤 Portfolio」
(`https://app.notion.com/p/2026-Portfolio-0009d4ab6bba82c5b160816e3f7c494a`) 원문을 **한 글자도 고치지 않고** 옮긴 것이다.
구조만 사이트 블록(h2·ul·callout·img-row·table)으로 바꿨다. 노션의 색 글자는 `<em>`, 배경색 글자는 `<span class="mark">`.
노션에만 있는 것 — 프로토타입 mp4 2개, 분석서 PDF·xlsx 첨부 — 는 API로 받을 수 없어 노션 원문 링크(`note()`)로 안내한다.
다시 만들 일이 있으면 스크립트를 새로 쓰기보다 `data.js`를 직접 고치는 편이 빠르다.
이미지는 폭 2000px 이상이면 2000px으로 줄여 넣었다(문서 스크린샷이라 그 이상은 의미가 없다).

## 데이터 스키마

`data.js`는 전역 두 개만 정의한다.

```js
window.PORTFOLIO_PROFILE = {
  name: "이정훈",
  title: "게임 기획자 포트폴리오",
  tagline: "한 줄 소개",
  cards: [ { label: "직무", value: "시스템 · 컨텐츠 기획" }, ... ]  // value에 HTML 허용
};

window.PORTFOLIO_UNITS = [
  {
    title: "개발 프로젝트",
    layout: "gallery",            // 아래 표 참고. 생략 시 "cards"
    mids: [
      {
        title: "외(계인)노(동)자",
        meta: "2025.03 ~ · 시스템 & 컨텐츠 기획",   // 선택
        cover: "images/cover-01-x.png",             // 선택. 편집 중에는 data:image/... 형태
        coverName: "x.png",                         // 내보낼 때 파일명 힌트
        lessons: [
          {
            title: "프로젝트 소개",
            tag: "개요",                            // 선택
            links: [ { label: "▶ 플레이 영상", url: "https://..." } ],  // 선택
            html: "<p>본문 HTML</p>"
          }
        ]
      }
    ]
  }
]
```

3단 위계는 **섹션(unit) → 그룹(mid) → 문서(lesson)** 이다. 용어를 바꾸지 말 것.

### layout 값

| 값 | 홈에서의 표시 |
|---|---|
| `gallery` | 하위 탭에 썸네일이 붙는 프로젝트형 |
| `cards` | 기본값 |
| `list` | 간단한 목록 |
| `inline` | About Me처럼 내용을 그대로 펼침 |

현재 배정: `0 About Me = inline`, `1 개발 프로젝트 = gallery`,
`2 메이플스토리 기획 문서 = cards`, `3 대외 활동 = list`.

## index.html 구조

- **상단 배너(`.mast`)** — `banner.png`를 배경 띠로 깔고 위에 이름·소개·테마·편집 버튼.
- **상단 탭(`.tabbar`)** — 섹션 전환. sticky.
- **하위 탭(`.subtabs`)** — 그룹이 2개 이상일 때만. 썸네일 원형 + 이름 + 문서 수.
- **본문(`.pane`)** — 선택된 그룹의 **모든 문서를 펼쳐서** 표시한다.
  카드를 눌러 상세로 들어가는 뎁스는 의도적으로 제거했다. 되살리지 말 것.
- About Me 탭에서만 `.about` 프로필 블록(사진 + 정보 칩 + 지표)을 렌더한다.
- 라우팅은 해시 `#섹션-그룹` (예: `#1-0`). 그 외 형식은 없다.

## admin.html 구조

- 좌측 트리: 섹션/그룹/문서. 문서 줄은 **`div[role=button]`** 이다.
  중첩 `<button>`은 브라우저가 DOM을 재구성해 클릭이 깨지므로 **절대 쓰지 말 것.**
- 우측: 제목·태그·링크 폼 + contenteditable 본문 편집기.
- 이미지를 클릭하면 `#imgTools` 의 도구막대가 **이미지 안쪽 가운데 위**에 얹힌다(낮은 이미지면 위로).
  `#imgTools` 는 `position:fixed; inset:0` 이어야 한다 — 안의 막대·핸들이 `absolute` 라 컨테이너가 뷰포트를 덮어야 한다.
- 본문 왼쪽 `#blkRail`에 블록마다 손잡이 두 개를 절대배치한다 — `＋`(아래에 블록 추가)와
  `⠿`(드래그 이동 / 클릭하면 도구). 블록 = `#editor`의 직계 자식.
- 작업 내용은 `localStorage["portfolio-draft-v1"]`에 자동 보관된다.
- 위쪽 `? 도움말`(F1)이 사용법 전체다(`HELP_HTML`). 기능을 더하면 여기도 같이 고친다.

### 되돌리기 / 다시 실행

브라우저의 undo 는 `execCommand` 로 바꾼 것만 기억한다. 블록 삭제·이동·표 편집·이미지 배치처럼
DOM 을 직접 고친 작업은 남지 않으므로, 편집 칸 전체를 스냅샷(`hist.past / present / future`)으로 남겨 우리가 되돌린다.

- `histRecord(kind)` 를 **모든 변경 경로**가 부른다 — `flush()`, `saveBlocks()`, `ed.oninput`. 새 조작을 만들면 이 셋 중 하나를 거치게 할 것.
- 연속 타이핑(`inputType` 이 insertText/deleteContent…)은 1초 안이면 한 단계로 묶인다. 그 밖은 한 단계씩.
- 선택 영역은 `caretPath()` 로 편집 칸 기준 경로를 적어 두고 `restoreCaret()` 으로 되찾는다. `innerHTML` 을 갈아 끼우면 노드 참조가 전부 죽기 때문.
- 문서를 바꾸면 `histInit(sel)` 이 이력을 새로 시작한다. 같은 문서를 다시 그린 경우(링크 칸 수정)는 잇는다.
- Ctrl+Z/Y 는 `document` 에서 받는다 — 블록 도구를 누른 뒤 포커스가 빠져 있어도 되돌아가야 하므로. 글 입력칸과 모달 안은 브라우저에 맡긴다.
- 브라우저 undo 와 섞이면 두 번 되돌아가므로 편집기 안에서는 항상 `preventDefault`.

### 여러 블록 범위 선택의 시작 지점

`bandStartOK(e)` — 레일, 편집 칸 좌우의 어두운 여백, 본문 아래 빈 곳(`.main` 안이되 `#editor`·입력칸·버튼·도구막대 밖)이면 어디서든 끌어서 고를 수 있다.

### 왼쪽 트리

- 위쪽 고정 줄에 **검색**과 **모두 접기/펼치기**. 검색 중에는 접힘을 무시하고 걸린 문서만 남긴다.
- 섹션·그룹은 `▼`로 접힌다. 처음 열 때는 **지금 보고 있는 그룹만** 펼쳐 둔다.
- 접힘 상태는 `WeakSet folded`에 **객체 자체로** 물려 둔다.
  인덱스로 기억하면 순서를 바꾸는 순간 엉뚱한 항목이 접히고,
  객체에 필드로 붙이면 `JSON.stringify(units)`를 타고 **data.js로 새어 나간다.**
- **드래그로 정렬** — 문서는 다른 그룹으로, 그룹은 다른 섹션으로 옮길 수 있다.
  위/아래 절반이면 앞뒤로(`tree-before`/`tree-after`), 그룹·섹션 머리 한가운데면 그 안으로(`tree-into`).
  키보드는 문서 줄에서 **Alt+↑↓**.
- 순서를 바꾼 뒤 열려 있던 문서는 `selObj()` → `refindSel()`로 **객체를 다시 찾아** 따라간다.
  인덱스를 손으로 보정하면 이동 경우의 수마다 깨진다.
- 제목 칸을 누르는 동안에는 부모의 `draggable`을 꺼 둔다. 안 그러면 글자를 끌어 고를 수 없다.
- 오른쪽 경계선(`#sideGrip`)을 끌면 폭이 바뀌고(`--side-w`, localStorage `pf-side-w`),
  더블클릭하거나 위쪽 `☰ 목록` 버튼을 누르면 통째로 접힌다.
- `renderSide()`는 스크롤 위치를 지켰다가 되돌린다. 다른 문서로 옮겨 갔을 때만 그 줄을 화면 안으로 끌어온다.

### 편집 지면은 index.html과 같은 모양이어야 한다

`.editor`에 index.html의 `:root` 토큰(`--bg` `--ink` `--accent` `--s1~--s7` …)과 `.doc` 타이포를
그대로 옮겨 두었다. 편집 칸 폭은 `--doc-w`(680px ≒ 사이트의 `70ch` + 여백)이고,
`[data-theme="dark"] .editor`가 사이트의 다크 값을 받는다.

> **index.html의 색·간격·타이포를 고치면 admin.html의 `.editor` 블록도 같이 고칠 것.**
> 두 곳이 어긋나면 "편집 화면 = 결과 화면"이라는 전제가 깨진다.

### 노션식 입력

| 입력 | 결과 |
|---|---|
| `/` | 블록 목록(`#slashMenu`). 이어 치면 이름·키워드로 걸러진다. ↑↓ · Enter · Esc |
| `# ` `## ` `### ` | 큰 제목 / 소제목 |
| `- ` `* ` `+ ` `1. ` | 글머리 목록 / 번호 목록 |
| `> ` `" ` | 강조 박스 / 인용구 |
| `---` + Enter | 구분선 |
| 글자 선택 | 위에 서식 툴바(`#selBar`) — 굵게·형광·색강조·링크·제목 |
| Ctrl+D / Ctrl+K / Ctrl+E | 블록 복제 / 링크 / 형광펜 |
| `A−` `A+` · 크기 칸 · `기본` | 글자 크기(px). 칸에 숫자를 치고 Enter 하거나 목록에서 고른다. Ctrl+Shift+`.` / `,` 로 단계 이동 |

글자 크기는 `<span style="font-size:Npx">` 로 남는다(`applyFontSize`). `execCommand("fontSize", 7)` 이 남기는
`<font size="7">` 을 span 으로 바꾸는 방식이라 굵게·링크 같은 안쪽 서식이 살아남는다.
크기 입력칸은 포커스를 받아야 하므로 `#selBar` 의 mousedown 에서 선택 영역을 `sizeRange` 에 복사해 두고,
`updateSelBar()` 는 입력칸에 커서가 있는 동안 툴바를 숨기지 않는다. `cleanPaste()` 도 px 크기만은 살린다.

블록 삽입 템플릿의 `[data-focus]` 안내 문구는 **선택된 채로** 들어간다. 바로 쳐서 덮어쓰라는 뜻이다.

### 여러 블록 한꺼번에 다루기

고르는 방법은 셋이고 결과는 같다 — `blkSel` 배열에 최상위 블록들이 담기고 `.blk-sel`이 붙는다.

1. 글자를 끌어 **두 블록 이상에 걸치면** 블록 선택으로 승격된다(`syncBlkSel`). 노션과 같은 방식.
2. 본문 왼쪽 **레일 빈 곳을 끌면** 범위로 고른다(`#blkBand`).
3. 손잡이(`⠿`)를 **Shift로 누르면** 먼저 고른 블록까지 넓어진다.

고른 뒤에는 `#blkTools`가 개수를 띄우고 **↑↓ 이동 · 종류 · 복제 · 삭제**가 전부 여러 개에 적용된다.
손잡이 하나를 끌면 고른 것 전부가 같이 움직인다(`dragPack`). 키보드는 **Delete · Ctrl+D · Alt+↑↓ · Esc**.

- `blkManual` — 레일에서 직접 고른 선택은 글자 선택이 풀려도 유지된다.
  이게 없으면 손잡이를 누르는 순간 `selectionchange`가 선택을 지워 버린다.
- `selectionchange`는 **`syncBlkSel()` → `updateSelBar()` → `markEmpty()`** 순서로 부를 것.
  순서가 틀리면 글자 툴바가 한 박자 늦은 상태를 보고 블록 툴바와 겹쳐 뜬다.
- **종류 바꾸기**(`turnInto`)는 `innerParts()`로 알맹이만 뽑아 `makeBlocks()`로 다시 만든다.
  목록·상자·인용구로 바꿀 때는 고른 블록들이 하나로 합쳐지고, 제목·본문으로 바꿀 때는 각각 남는다.

### 표 편집

셀 안에 커서가 들어가면 `#tblTools`가 표 위에 뜬다 — 행 위/아래 추가·삭제, 열 좌/우 추가·삭제,
첫 줄을 제목 행(`th`)으로 토글. 표를 다시 만들 필요가 없다.

### 이미지 병렬 배치

- 여러 장은 `<div class="img-row">` 안에 나란히 넣는다. 한 줄 최대 4장(`MAX_ROW`).
- **Ctrl+V** — 클립보드에 이미지가 여러 장이면 한 줄로 들어간다.
  이미지를 **고른 상태**로 붙여넣으면 그 이미지의 줄에 이어 붙는다
  (단독 이미지였다면 그 자리에 줄을 만들어 나란히 놓는다).
- **Ctrl+C / Ctrl+X** — 고른 이미지를 `imgClip`에 담는다. 브라우저 클립보드에는 넣지 않는다.
  편집기 안에서만 오가는 값이라 텍스트 클립보드를 더럽히지 않는다.
- **이미지를 직접 끌어서** 다른 이미지의 좌·우 절반에 놓으면 그 줄에 나란히 배치된다.
  같은 줄 안에서는 순서 바꾸기가 된다. `⠿` 손잡이를 잡지 않아도 된다.
  이미지가 아닌 곳에 놓으면 그 블록의 위/아래로 옮겨진다(= 줄에서 빼내기).
- `⠿` 손잡이로 이미지 블록(여러 개 골라도 됨)을 끌어 다른 이미지의 **좌·우 바깥 1/3**(`MERGE_ZONE`)에 놓으면
  그 이미지 옆으로 합쳐진다. 가운데 1/3 은 위/아래로 옮기는 자리. 대상이 이미지 줄이면 포인터 아래 이미지가 기준이다.
- **Alt+←/→** 또는 이미지 도구막대의 `◀ ▶` — 줄 안에서 좌우 이동.
- 외부에서 복사해 온 서식은 `cleanPaste()`가 벗긴다. `src`가 `data:`가 아닌 이미지는
  **주소가 곧 만료되므로 빼고** 파일로 붙여넣으라고 알린다(노션 서명 URL 문제).

## 저장 파이프라인

1. 편집 중 이미지는 `data:image/...;base64,` 로 문서 HTML에 인라인된다.
2. 저장 시 `buildSplit()`이 이를 뽑아내 `images/파일명`으로 경로를 바꾸고 파일 목록을 만든다.
3. 저장 방식 세 가지
   - **GitHub 커밋** (기본): blob → tree → commit → ref PATCH. 토큰은
     `localStorage["pf-github"]`에 저장되며 파일에는 절대 들어가지 않는다.
   - **ZIP**: 외부 라이브러리 없이 직접 구현한 무압축 ZIP writer 사용.
   - **data.js 단독 다운로드**.
4. `버전` 기능은 IndexedDB(`pf-editor`)에 최대 10개 스냅샷. IndexedDB가 막힌 환경
   (`file://`)에서는 자동으로 localStorage로 대체된다.

## 절대 지켜야 할 제약

- **외부 라이브러리·CDN·빌드 도구 금지.** 모든 코드는 두 HTML 파일 안에 인라인.
- **파일을 더 쪼개지 말 것.** 사용자가 GitHub 웹 UI로 직접 업로드하기 때문에
  파일 개수가 늘면 운용이 불가능해진다.
- `data.js`는 편집기가 생성한다. 손으로 고치는 코드를 넣지 말 것.
- 사용자 콘텐츠(`lesson.html`)를 렌더할 때 `esc()`를 씌우면 안 된다. 이미 HTML이다.
  반대로 **미리보기 텍스트를 만들 때는 `textOf()`로 DOM 파싱해 엔티티를 풀 것.**
  정규식으로 태그만 지우면 `&amp;`가 화면에 노출된다.
- 이미지 관련 코드는 `.skill-ico`(카드 아이콘)를 대상에서 제외해야 한다.

## 과거에 실제로 터진 버그 — 회귀 방지

| 증상 | 원인 |
|---|---|
| 트리에서 문서를 클릭해도 안 열림 | `<button>` 안에 `<button>` 중첩 |
| `422 Update is not a fast forward` | 커밋 시 head가 앞서감. 재시도 루프로 해결 |
| `textB64 is not defined` | 함수 정의는 `textToB64`인데 호출은 `textB64` |
| 카드에 `&amp;` 노출 | 정규식 태그 제거 + `esc()` 이중 적용 |
| 순서 이동 시 편집 내용 소실 | 구조 변경 전 `flush()` 누락 |
| 폴더 저장·버전 기능 무반응 | `file://`에서 File System Access API와 IndexedDB 차단 |
| `## ` 다음 줄에서 `- ` 를 치면 목록이 **앞의 제목을 통째로 삼킴** | 빈 블록에서 `execCommand("insertUnorderedList" / "formatBlock")` 호출. 브라우저가 캐럿을 앞 블록으로 정규화한다. → `swapBlock()`으로 DOM을 직접 갈아 끼운다 |
| 빈 블록으로 바꾼 뒤 친 글자가 한 칸 밀림 | 남겨 둔 `<br>` **뒤에** 캐럿을 놓아서. 빈 블록이면 `setStart(box, 0)`으로 **앞에** 놓을 것 |
| 저장본에 `class="img-sel"` `class=""` 가 섞여 나감 | `ed.innerHTML`을 그대로 저장했음. → `edHtml()`이 편집용 클래스를 벗겨서 돌려준다 |
| 엔터마다 `<div>`가 생겨 사이트의 문단 여백이 안 먹음 | `defaultParagraphSeparator` 미설정. 편집 칸에 **포커스가 있을 때** 걸어야 먹는다 |
| 트리에서 순서를 바꾸면 접혀 있던 항목이 엉뚱하게 바뀜 | 접힘을 인덱스로 기억해서. → `WeakSet`에 객체로 물릴 것 |
| 트리 제목 칸에서 글자를 끌어 고를 수 없음 | 부모에 `draggable="true"`가 걸려 있어서. 입력칸을 누르는 동안 꺼 둘 것 |
| 손잡이를 누르는 순간 블록 선택이 풀림 | `selectionchange`가 뒤늦게 와서 지운다. → `blkManual` 플래그로 보호 |
| 블록 삭제·표 편집이 Ctrl+Z 로 안 돌아옴 | 브라우저 undo 는 execCommand 만 기억한다. → 스냅샷 이력(`hist`)으로 직접 되돌림 |
| 서식 툴바 버튼이 두 번 실행돼 굵게가 도로 풀림 | `bindForm` 이 `[data-cmd]` 전체에 onclick 을 걸어 `#selBar` 버튼까지 묶였음. → `.toolbar [data-cmd]` 로 한정 |
| 이미지 도구막대가 이미지와 상관없는 곳에 뜸 | `#imgTools{position:fixed}` 에 `inset` 이 없어 정적 위치(문서 맨 아래)에 놓였고, 안의 `absolute` 막대가 거기를 기준으로 잡힘. → `inset:0` |

## 디자인 토큰

CSS 변수는 `:root`에 정의. 다크가 기본이다.

```
배경 #000000 / 카드 #0c0c0c / 테두리 #1f1f1f
글자 #f2f5f4 · #9ea6a4 · #6b7271
키컬러 #3ecfb2 (민트)   보조 #45b8d1 (청록)
```

키컬러와 보조는 **같은 계열로 유지**한다. 과거 골드(`#d9a441`)를 보조로 썼다가
민트와 충돌해 되돌렸다. 난색은 오류 표시(`--danger`)에만 쓴다.
폰트는 Pretendard 우선, 웹폰트 로드는 하지 않는다(설치돼 있으면 사용).

## 작업 시 주의

- 변경 후에는 반드시 `index.html`과 `admin.html`의 인라인 스크립트가
  파싱되는지 확인할 것. (`new Function(scriptBody)`)
- 사용자에게 파일을 전달할 때는 **무엇이 바뀌었고 어떤 파일만 다시 올리면 되는지**
  명시할 것. 특히 `data.js`를 덮어쓰면 사용자가 작성한 내용이 사라진다.
- UI 문구는 한국어. 전문용어보다 일상어를 쓴다.

## 남은 작업

- [x] 프로필 사진 — 노션 원본을 `profile.jpg` 로
- [x] 프로젝트·문서 `cover` 썸네일 — 각 노션 페이지의 첫 이미지
- [x] 노션 원본의 기획안 스크린샷 전부 삽입 (52장)
- [ ] 노션 첨부 파일(PDF·xlsx·mp4)을 저장소에 직접 넣기 — 지금은 노션 링크로 안내
- [ ] 업로드된 `2026_메커톤_포트포리오_게임기획_이정훈.pdf`와 내용 대조 (미확인)
- [ ] 브라우저에서 편집기 실제 클릭 테스트 (이 환경에서는 자동화가 막혀 코드 검토만 했음)
